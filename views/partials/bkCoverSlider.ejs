<div id="carouselExampleIndicators<%=id%>" class="carousel slide bookgallery" data-ride="carousel">
    <ol class="carousel-indicators">
      <li data-target="#carouselExampleIndicators<%=id%>" data-slide-to="0" class="active"></li>
      <%gallery.forEach(function(imageuri,i){%>
        <%if(i !== 0){%>
          <li data-target="#carouselExampleIndicators<%=id%>" data-slide-to="<%=i%>"></li>
        <%}%>
      <%})%>
    </ol>
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img class="" src="/public/bookcovers/<%=gallery[0]%>">
          </div>
      <%gallery.forEach(function(imageuri,i){%>
        <%if(i !== 0){%>
          <div class="carousel-item">
              <img class="" src="/public/bookcovers/<%=imageuri%>">
            </div>
        <%}%>
      <%})%>
    </div>
    <br>
    <a class="carousel-control-prev" href="#carouselExampleIndicators<%=id%>" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators<%=id%>" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>